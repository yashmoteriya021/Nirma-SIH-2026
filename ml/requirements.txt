# SIH PS 26092 — AI-Driven Scheme Matching Pipeline + FastAPI service

# Core
pydantic>=2.0,<3.0        # Request/response models for the service
python-dateutil>=2.8      # Date parsing
python-dotenv>=1.0        # Loads ml/.env

# Service
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27               # FastAPI TestClient

# LLM providers (Module 2). Only the one you configure is used at runtime.
openai>=1.0               # OpenAI, OpenRouter, Ollama (OpenAI-compatible)
anthropic>=0.40           # Anthropic Claude

# Text-to-speech (Module 2). Free, no API key (wraps Edge's neural voices).
edge-tts>=6.1

# Tests
pytest>=7.0
